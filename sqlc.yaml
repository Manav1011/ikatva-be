version: "2"
sql:
  - schema: "migrations/"
    queries: "internal/"
    engine: "postgresql"
    gen:
      go:
        package: "db"
        out: "internal/db/gen"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
        emit_exact_table_names: false
